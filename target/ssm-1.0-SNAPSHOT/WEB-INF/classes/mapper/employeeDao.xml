<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hc.dao.EmployeeDao">
    <select id="queryAllEmp" resultType="Employee">
        SELECT emp_id,emp_name FROM employee;
    </select>
</mapper>